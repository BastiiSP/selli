*.iml
.gradle/
.kotlin/
/local.properties
/.idea/
.DS_Store
build/
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab

# Enthält vertrauliche Werte (u.a. den ICS-Feed-Link mit Zugriffstoken) — niemals committen.
secrets.properties

# Rohausgabe der Bildgenerierung — erst manuell kuratierte Assets landen unter res/.
/tools/generate-assets/output/

# Private Referenzfotos für die Avatar-/Maskottchen-Generierung — nie committen.
/tools/generate-assets/reference-photos/

# Enthält den OpenAI-API-Key fürs Asset-Tool — niemals committen.
/tools/generate-assets/.env
# Editor-Sicherungsdateien (z. B. von nano) könnten Reste vertraulicher Werte enthalten.
*.save

# Lokale Brainstorming-Mockups (superpowers:brainstorming Visual Companion) — kein Produktionscode.
.superpowers/
