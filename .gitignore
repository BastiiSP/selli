*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
build/
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab

# Enthält vertrauliche Werte (u.a. den ICS-Feed-Link mit Zugriffstoken) — niemals committen.
secrets.properties

# Rohausgabe der Bildgenerierung — erst manuell kuratierte Assets landen unter res/.
/tools/generate-assets/output/

# Enthält den OpenAI-API-Key fürs Asset-Tool — niemals committen.
/tools/generate-assets/.env
